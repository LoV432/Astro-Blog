---
const { imageObject, classes, size, alt } = Astro.props;
import imageHelper from '../lib/image-helper';
let image = imageHelper(imageObject, size);
---

<img width={image.width} height={image.height} src={image.src} class={classes} alt={alt} />
